import Promise = require('native-or-bluebird');
import { DependencyTree } from './interfaces/main';
export interface InstallDependencyOptions {
    save?: boolean;
    saveDev?: boolean;
    ambient?: boolean;
    name?: string;
    cwd: string;
    source?: string;
}
export interface InstallOptions {
    cwd: string;
    production: boolean;
}
export declare function install(options: InstallOptions): Promise<DependencyTree>;
export declare function installDependency(dependency: string, options: InstallDependencyOptions): Promise<DependencyTree>;
